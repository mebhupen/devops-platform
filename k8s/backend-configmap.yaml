apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: enterprise-devops

data:
  PORT: "5000"
  NODE_ENV: "production"

  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: task_management
  DB_USER: postgres

  JWT_EXPIRES_IN: 1d
